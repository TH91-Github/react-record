import styled from "styled-components"

// 로그인 on/off
export const UserMenu = ({login}:{login:boolean}) => {
  return ( 
    <StyleWrap>
      {
        
      }
    </StyleWrap>
  )
}
const StyleWrap = styled.div`

`;